---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 57749794cdfeb9994825e9b2553efdcf_ce6ad4b4aced11f18f50525400aeaaa3
    ReservedCode1: wkum9wUqQMTZRLjH0KaSQH8m1V8NVI1Y7IpEQ28SmsXc0iQ125P8HmWJTFsuWwAocqOQwpmH8f7hmL68Iwpu01KL7k9H0LeJ5YaM6g/Nj64kSTeZnNoK1Ce0+qOlc08yme6DqAZeloGjzu6EUr3FtK0F1/uhGy+ZuJ8CYoQhoxwaKlfj1hmEDf3le54=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 57749794cdfeb9994825e9b2553efdcf_ce6ad4b4aced11f18f50525400aeaaa3
    ReservedCode2: wkum9wUqQMTZRLjH0KaSQH8m1V8NVI1Y7IpEQ28SmsXc0iQ125P8HmWJTFsuWwAocqOQwpmH8f7hmL68Iwpu01KL7k9H0LeJ5YaM6g/Nj64kSTeZnNoK1Ce0+qOlc08yme6DqAZeloGjzu6EUr3FtK0F1/uhGy+ZuJ8CYoQhoxwaKlfj1hmEDf3le54=
---



# MSA · 分析建议

## 何时使用

- 用户报出一个股票代码（如 `600519`、`000001`、`300750`）要求分析、盯盘、预判涨跌、给持仓建议。
- 用户报出 ETF / 基金代码（如 `510300` 沪深300ETF、`159915` 创业板ETF、`161725` 白酒LOF）要求分析、盯盘、预判、给持仓建议。
- 用户报出代码 + 持仓数量 / 成本价，要求"该不该加仓、减仓、止损"。
- 用户要求"隔 1 分钟分析一次""每 1 分钟看一下"。

## 环境与依赖

- 脚本目录：本 skill 的 `scripts/`
  - `msa_core.py`：数据采集 + 指标计算 + 预判模型 + Markdown 渲染。
  - `msa.py`：命令行入口（单次分析 / 轮询）。
  - `msa_env.py`：运行容器（客户端）自动识别，供面板标题使用。
  - `msa_events.py`：基本面与突发事件参数（东方财富全文检索新闻 + 基金基本面 + 大盘环境），供面板纪律线开关按需联网调用。
  - `msa_server.py` + `web/index.html`：Web 动态面板（默认随调用唤起）。
- 数据源：同花顺 `thsdk` 游客模式，实时行情 + 日线/周线/月线 + 5 分钟线 + 分时 + 逐笔 + 五档 + 资金流 + 问财字段。
  - 沪深 A 股：全字段直取。
  - ETF / 基金：走基金专有问财字段（单位净值 / 参考净值 / 折溢价率 / 基金规模 / 基金份额 / 跟踪指数 等），资金流在无专用接口时由分价表 + 逐笔成交推导，涨跌幅限制按 10% / 双创类 20% 兜底识别。
- 如报 `ModuleNotFoundError: thsdk`，先 `pip install thsdk` 再重试。

## 面板客户端标识（动态识别）

面板的浏览器选项卡标题与页面第一行均形如 `MSA 实时分析面板 · <客户端名>`；客户端名在 skill 执行时**动态获取，不写死**，识别优先级：

1. 显式 `--client` 参数；
2. 环境变量 `MSA_CLIENT_NAME`；
3. 进程祖先链（谁调起解释器：Marvis / WorkBuddy / 豆包）；
4. 容器品牌环境变量前缀（`MARVIS_*` / `WORKBUDDY_*` / `DOUBAO_*`）；
5. skill 所在目录特征（`.marvis` / `.workbuddy` / `Doubao`）；
6. 兜底「Marvis 电脑端」。

面板运行中再次执行本 skill 时，会通过 `/api/config?client=` 热更新标题，因此标签始终显示**当前正在执行本 skill 的客户端**。

多客户端共存时面板按容器独占端口（从 8765 起顺延）：`/api/health` 会返回 `owner`（面板归属容器），脚本只复用归属自己的面板，避免 Marvis / WorkBuddy / 豆包共用同一端口导致标题或数据串台。

## 纪律线的「基本面 · 突发事件」开关（面板内）

- 位置：面板「纪律线」卡片标题栏，复选框文案「基本面 · 突发事件」；勾选状态写入浏览器 localStorage，刷新后保留。
- 未勾选：纪律线仅按技术面计算（均线 / 区间高低点 / 趋势轨 / ATR），注释明确标注"不含基本面与突发事件"。
- 勾选：面板实时请求 `/api/events?code=&name=&market=&refresh=1`，取得外部环境分（舆情事件 0.55 + 基本面 0.25 + 大盘环境 0.20 合成）后修正纪律线：
  - 止损：偏空收紧（上移）、偏多放宽，幅度随环境分强弱变化；
  - 止盈 / 减仓线：偏空下调、偏多上调；
  - 买入风险预算门槛 ×倍率：偏空抬高（门槛最高 2.2 倍），偏多降低（最低 0.55 倍），风险空间不足时仍不设买点。
  - 界面同步展示「外部环境」摘要框（舆情事件 / 基本面 / 大盘环境 / 突发事件冲击等级 / 相关报道条数 / 主题）与修正幅度说明。
- 每次勾选、重新分析或切换标的都会重新联网取数，参数随现实世界变化而变化（新闻检索在严格同义词过滤为空时会自动放宽为主题词命中即收录，避免退化为全中性）。
- 取消勾选即时回到纯技术面口径，无需刷新页面。

## 执行流程

1. 解析用户输入：证券代码（必填）、持仓数量 `--shares`（可选）、成本价 `--cost`（可选）。
   - 代码无需区分股票或 ETF/基金，脚本自动识别标的类型；也无需指定 `--market`。
   - 用户没给持仓数量时照常分析行情与预判，报告不渲染持仓建议段落。
2. 单次分析（把 `--out` 指向**结果产物目录**）：

   ```powershell
   python "<skill_dir>\scripts\msa.py" --code 600519 --shares 1000 --cost 1200 --out "<output_dir>"
   ```

3. 分钟级轮询（用户要"隔 1 分钟分析一次"时）：

   ```powershell
   python "<skill_dir>\scripts\msa.py" --code 600519 --shares 1000 --cost 1200 --watch --interval 60 --rounds 5 --calm --out "<output_dir>"
   ```

   - `--interval 60` 即每 1 分钟一次；`--rounds 0` 表示不限次（需配合中断）。
   - `--calm` 仅在评分方向变化时打印完整摘要，日志始终追加到 `msa_watch_YYYYMMDD.log`。
   - 轮询时长超过单次工具调用上限时，改用 `create_scheduled_task`（interval/cron）定时触发单次分析，不要长时间占用一次调用。
4. 读取脚本输出的 `compact_summary`，把生成的 `msa_<代码>.md` 报告路径用 `yyb-product` 卡片交付；如用户需要关键结论，直接在回复中复述摘要中的行情、预判与持仓建议。

## 报告结构（Markdown）

| 章节 | 覆盖字段 |
| --- | --- |
| 一、行情快照 | 今开 / 最高 / 最低 / 昨收 / 现价 / 涨跌幅 / 涨速 / 均价 / 总手 / 金额 / 量比 / 换手率 / 振幅 / 涨停价 / 跌停价 / 委比 / 委差 / 内盘 / 外盘 |
| 二、技术指标 | MA5/10/20/60/120、DIF、DEA、MACD(M)、KDJ-K/D/J、RSI6/12/24、ATR14 及 ATR14%、量比、今昨成交比、近 20/60 日高低与距离 |
| 三、盘口与资金 | 买 1-5 / 卖 1-5 五档、大单模式（五档挂单）、均价、买卖力道、买卖差、内盘/外盘、主力净额、大单净量、大单净额、DDX/DDY 等资金流、大单（≥100 万）、分价表（主买/主卖占比、分时博弈） |
| 四、周期表现 | 7 / 14 / 28 / 57 日区间涨跌幅、振幅、日均量、日均额、区间高低；近 8 根 5 分钟K线；盘后固定价格适用性说明 |
| 五、估值与股本 / 基金专有数据 | 个股：市盈率 / 市净率 / 市销率 / 总市值 / 流通市值 / 股息率 / 总股本 / 流通股本 / 每股净资产 / 实时估值；ETF 与基金：单位净值 / 参考净值 / 折价率（折溢价率）/ 基金规模 / 基金份额 / 跟踪指数，规模缺失时按份额×净值估算并标注"约" |
| 六、日/周/月涨幅预判与趋势线 | 每周期给出方向（偏多/震荡/偏弱）、涨跌幅区间、趋势线斜率与上下轨、综合评分与评分依据 |
| 七、持仓情况与建议 | 持仓数量 / 成本价 / 现价 / 市值 / 浮动盈亏 / 盈亏比例 / 综合评分 / 止损参考位 / 止盈参考位 / 建议动作 / 建议目标仓位 |

## 预判模型说明（回复用户时如实交代）

- 日线预判：日内量价（量比、今昨成交比、均价位置、收盘位置）+ 五档买卖力道 / 买卖差 + 资金流（主力净额占比、大单净额）+ 分价主买占比 / 分时博弈 + 趋势与 MACD/KDJ/RSI 位置 → 评分 → 按 ATR14% 折算涨跌幅区间。
- 周线预判：周/14 日趋势斜率 + MACD/KDJ 周级位置 + 量能变化。
- 月线预判：28/57 日趋势与均线多空排列 + 位置高低 + ATR 放大区间。
- 结论为量化评分推算，非投资建议；务必在输出中提示风险，不做收益承诺。

## 已知边界（务必如实告知，不要编造数据）

- **沪深 A 股**（600/601/603/605/000/001/002/003/300/301/688/689 开头）为全字段直取。
- **沪深 ETF / 场内基金**（510/511/512/513/515/516/518/588/159/161/162/163/164/165/166/168/560/561/562/563 等）已支持：行情与 K 线直取，专有数据走基金问财字段；基金无专用资金流接口时，由分价表 + 逐笔成交推导主力净额等近似值，报告中标注口径。
- ETF / 基金的涨跌幅限制由涨停价与昨收反推；创业板 / 科创板系列 ETF 按 20% 兜底，其余按 10%。
- **北交所、港股**部分接口仍无数据，脚本会直接报错说明，不要臆造。
- `盘后固定价格成交量/额`：仅科创板/创业板股票适用，游客模式无该字段权限，报告中会显式标注"未取到"，不要臆造数值。
- 逐笔成交为抽样计算（脚本标注"抽样"与样本量），极端小盘股样本可能不足。
- 非交易时段运行时，行情为最近一个交易日收盘数据，脚本会在报告头部标注"行情日期"。
*（内容由AI生成，仅供参考）*
